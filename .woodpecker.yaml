# Runs whenever a push occurs on either target branch
when:
  - event: push
    branch: codeberg-staging
  - event: push
    branch: main

steps:
  validate-pretext:
    image: pretextbook/pretext:latest
    commands:
      - pretext build html
    when:
      branch: codeberg-staging

  build:
    image: pretextbook/pretext:latest
    commands:
      - pretext build course
    when:
      branch: main

  deploy:
    image: pretextbook/pretext:latest
    commands:
      - git config --global --add safe.directory /woodpecker/src || true
      - git config --global --add safe.directory "$(pwd)" || true

      - git config --global user.email "woodpecker-ci@noreply.codeberg.org"
      - git config --global user.name "Woodpecker CI"

      - echo "machine codeberg.org login token password $${CI_REPO_TOKEN}" > ~/.netrc
      - chmod 600 ~/.netrc

      - rm -rf deploy_dir

      - |
        # Ensure variables exist in the same shell that uses them
        TARGET_URL="https://codeberg.org/${CI_REPO}.git"
        BUILD_NUM="$${CI_PIPELINE_NUMBER}"

        # Clone pages branch if it exists, otherwise initialize it locally
        if git clone --single-branch --branch pages "$TARGET_URL" deploy_dir; then
          cd deploy_dir
          find . -maxdepth 1 -mindepth 1 ! -name ".git" -exec rm -rf {} +
          cd ..
        else
          mkdir -p deploy_dir
          cd deploy_dir
          git init
          git checkout -b pages
          cd ..
        fi

      - cp -ar output/course/. deploy_dir/

      - |
        cd deploy_dir
        git config --global --add safe.directory "$(pwd)" || true
        git add -A

        TARGET_URL="https://codeberg.org/${CI_REPO}.git"
        BUILD_NUM="$${CI_PIPELINE_NUMBER}"

        if git diff --cached --quiet; then
          echo "No changes to deploy. Skipping push."
        else
          git commit -m "Deploy textbook update - Build ${BUILD_NUM} [SKIP CI]"
          git push --force "$TARGET_URL" pages:pages
        fi
    when:
      branch: main
